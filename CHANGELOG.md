# Changelog

이 패키지의 주목할 만한 변경을 기록합니다.
형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/)를 따르고, 버전은 [유의적 버전](https://semver.org/lang/ko/)을 씁니다.

## [Unreleased]

아직 없습니다.

## [0.14.0] - 2026-08-23

**출시 감사에서 나온 것들을 고쳤습니다.** 대부분은 *조용히* 데이터를 잃던 자리입니다 — 오류도
경고도 없이 값이 바뀌거나 사라져서, 건수로도 리포트로도 드러나지 않던 것들입니다.
고친 자리마다 검사를 붙였습니다. (283건 → 326건)

**표를 시작하는 자리**를 하나 더했고, **말없이 데이터를 잃던 자리 둘**과 관계없는 임포트마다
프로젝트를 뒤지던 자리 하나를 고쳤습니다.

### Added

- **비공개 시트 인증이 이제 실제로 동작합니다.** `RSA.ImportPkcs8PrivateKey` 는 Unity 2022.3 의
  Mono 에서 **언제나** `PlatformNotSupportedException` 을 던집니다 — `RSA.Create()` 가 돌려주는
  `RSACryptoServiceProvider` 에 그 기능이 없습니다. 선언한 최소 판에서 유료 차별화 기능 하나가
  통째로 죽어 있었고, 이 영역의 검사가 0건이라 드러날 자리가 없었습니다.
  - `Pkcs8Rsa` 가 PKCS#8 DER 을 직접 풀어 `RSA.ImportParameters` 로 넣습니다. 그쪽은 어디서나 됩니다.
    제3자 라이브러리를 들이지 않았습니다 — 의존성 0 이라는 약속이 인증 하나보다 큽니다.
  - 검사 7건을 붙였습니다. 그중 하나는 **검사가 돌 때 만든 실제 2048비트 키로 서명하고 공개키로
    검증**합니다. 통과하면 그 에디터에서 비공개 시트가 됩니다. (개인키를 저장소에 넣지 않으려고
    키를 매번 새로 만듭니다)
  - `IsConfigured` 가 디스크 확인 결과를 경로 문자열로만 들고 있어 키 파일이 지워져도 계속
    "설정돼 있음" 으로 답하던 것을 함께 고쳤습니다. 화면은 인증이 살아 있다고 말하는데 받기는
    인증 없이 나가던 자리입니다.

- **지원 채널을 정했습니다 — 에셋스토어 제품 페이지의 리뷰.** `LICENSE.md` 에 남아 있던
  `<SUPPORT_EMAIL>` 자리 표시자와 `TODO(release)` 주석을 지우고 §13 을 넣었습니다.
  README 와 영문 매뉴얼에도 **Support** 절을 두어, 라이선스 전문을 열지 않아도 어디에 물어야
  하는지 보이게 했습니다.
  - 한 번에 답할 수 있는 보고에 무엇이 필요한지(표별 콘솔 로그 한 줄, Unity 판, 설치 방식)를
    함께 적었습니다. 없으면 세 번 주고받게 됩니다.
  - 좌석·환불·청구는 에셋스토어 EULA 가 다스리므로 Unity 쪽이라는 것도 갈라 두었습니다.
  - `인용` 제출 가이드라인 4.1.a 는 **판매자 프로필**에 활성 이메일과 유지되는 웹사이트를
    요구합니다. 제품별 고객 지원 창구를 강제하지는 않으므로 리뷰 채널로 충분하지만,
    프로필 쪽 요건은 별개로 남아 있습니다.

- **`.ci/check-language.py` 와 CI 검사.** 사람이 보는 자리에 한국어가 돌아오면 실패합니다 —
  문자열 리터럴, XML 문서 주석, README·매뉴얼·샘플, 그리고 채우지 않은 자리 표시자까지.
  **반쯤 번역된 상태가 번역하지 않은 것보다 나빴기 때문에** 그 상태로 되돌아가는 길을 막습니다.
  구현 주석과 이 변경 기록, LICENSE 의 한국어 요약은 그대로 두기로 한 것이라 보지 않습니다.
  검사기 자신은 `.ci/` 에 있어 배포본에 실리지 않습니다.

- **`.npmignore` 와 배포본 검사.** 아무것도 하지 않으면 UPM 배포본에 `.ci/`·`.claude/`·`.github/`
  와 **내부 검토 문서 5개**가 그대로 실려 나갑니다. `~` 로 끝나는 폴더가 안전하다고 오해하기 쉬운데,
  그것은 Unity 가 임포트하지 않는다는 뜻일 뿐입니다.
  - 검토 문서에는 경쟁 도구 가격 비교와 자기 약점 목록이 들어 있습니다. 없앨 문서가 아니라
    **상품에 들어가면 안 될** 문서입니다. 폴더를 통째로 빼고 구매자에게 갈 둘만 되살리는 방향이라,
    검토 문서를 하나 더 써도 저절로 빠집니다.
  - CI 에 `payload` 작업을 더했습니다. 들어가면 안 되는 것이 실렸는지, 있어야 할 문서·샘플이
    빠지지 않았는지, 치환하지 않은 자리 표시자가 남았는지를 봅니다. 검사가 전부 통과해도
    여기서 새면 상품이 망가집니다.

- **`CsvGuardTests`** — 위 수정들을 고정하는 검사 43건. 조용한 손실은 검사가 없으면 다음 판에서
  조용히 되돌아옵니다.
  - 그중 하나는 **참조 조사가 건너뛰는 확장자 목록**을 고정합니다. 그 목록에 참조를 담을 수 있는
    확장자가 하나 잘못 들어가면 그 파일이 붙잡고 있던 산출물이 지워지는데, 목록을 읽을 길이 없어
    검사할 수도 없었습니다. 검사 어셈블리에만 내부를 엽니다(`InternalsVisibleTo`).


- **타입에서 표 만들기.** ScriptableObject 에셋이나 그 스크립트를 고르면 필드를 읽어 열을 갖춘
  표를 뽑아 줍니다. 시트에 올려 저작을 시작하는 자리라, 프로젝트 안팎 어디에나 저장할 수 있습니다.
  `Project 창 우클릭 ▸ CSV Pipeline ▸ 이 타입의 표 만들기`, 또는
  `Tools ▸ CSV Pipeline ▸ 스크립터블 오브젝트로 표 만들기`.
  - **`[CsvAsset]`이 아직 없어도 됩니다.** 표를 만들기 전이라 선언이 없는 것이 정상입니다.
    붙어 있으면 그 선언(파일 이름·식별자 열·열 이름 지정)을 **그대로 따릅니다** — 여기서 지어낸 값이
    굽기와 갈라지면 사람이 보는 표와 굽는 표가 달라집니다.
  - **형식은 확장자가 정합니다.** `.csv`는 쉼표, `.tsv`는 탭입니다. 형식 고르기를 따로 두지 않은 것은
    이 패키지가 **표를 읽을 때 이미 확장자로 구분자를 정하기** 때문입니다. 두 자리를 두면 어긋납니다.
  - **이미 있는 에셋은 행으로 채웁니다.** 인스펙터에서 저작해 온 데이터를 시트로 옮기는 데 그대로
    씁니다. 하나도 없으면 헤더 한 줄만 나옵니다.
  - **되읽을 수 있는 열만 만듭니다.** `Rect`·`AnimationCurve`처럼 `CsvValueBinder`가 다루지 못하는
    필드는 빼고 **뺐다는 사실과 이유를 알립니다.** 열로 만들어 줬다면 사람은 시트에 그것을 채우고,
    받아오는 순간 행마다 경고가 쏟아집니다. 저작 가능 판정(`CsvValueBinder.CanAuthor`)은 변환기의
    갈래와 **같은 목록이어야** 하며, 어긋나면 검사가 잡습니다.
  - **선언이 없으면 붙여 넣을 `[CsvAsset]` 한 줄을 함께 줍니다.** 표만 만들어 두고 선언을 잊으면
    그 표는 아무것도 굽지 않습니다. 대화상자는 닫히면 사라지므로 콘솔에도 남깁니다.
  - 같은 이름의 표가 이미 있으면 **거기 적힌 열 표기를 그대로** 씁니다. 다시 뽑을 때마다
    `MaxSpeed`가 `maxSpeed`로 바뀌면 내용이 같아도 시트와 헤더가 어긋나 동기화가 멈춥니다.
- **굽기 뒤 요약 알림(`CsvBakeAlert`).** 파이프라인 창에서 `지금 굽기` 로 직접 구웠을 때, 오류나
  경고가 있으면 **한 번** 대화상자로 알립니다. 콘솔을 열어 두지 않으면 리포트를 놓치던 자리입니다.
  **자동 임포트에는 붙이지 않습니다** — 굽기 코드는 자동 임포트뿐 아니라 창을 새로 그릴 때마다
  도는 **미리보기**(`Plan`)와 배치로 도는 **드리프트 검사**도 함께 지납니다. 값마다 창을 띄우면
  표 목록을 보는 것만으로 대화상자가 뜨고, CI는 누를 사람이 없어 멈춥니다.
  배치 실행에서는 아예 뜨지 않습니다.
- **`CsvSchema.Draft`** — 선언이 없는 타입에서도 열 연결을 뽑습니다. 선언이 있으면 그것을 씁니다.
  **이것으로 굽지 마십시오.** 지어낸 선언은 굽기가 참고하지 않아 실제 임포트와 다른 결과가 납니다.
- **표시가 붙은 것만 받는 방식(`AutoMap = false`)을 일급으로 끌어올렸습니다.** 기능 자체는 v0.4.0부터
  한 줄로 있었지만 **검사가 0건이었고 문서도 표의 한 칸뿐**이라, 쓰라고 권할 수 있는 상태가 아니었습니다.
  - **`CsvSchema.OptIn` · `CsvSchema.UntaggedFields()`** — 표시가 없어 빠진 필드를 물어볼 수 있습니다.
    넣을 것만 적는 방식의 값은 *잊으면 빠질 뿐*이라는 데 있는데, 그 침묵이 동시에 유일한 위험입니다.
    `[CsvIgnore]`가 붙은 것은 **잊은 것이 아니라 정한 것**이라 이 목록에서 빠집니다.
  - **표 만들기가 "표시만 붙이면 열이 되는" 필드를 따로 알립니다.** 못 써서 뺀 것(`Omitted`)과는
    다른 목록(`Untagged`)입니다. 같은 말로 적으면 **고칠 수 있는 것과 없는 것이 한 덩어리로** 읽힙니다.
  - **표시를 하나도 붙이지 않은 타입**으로 표를 뽑으려 하면, 식별자 열 하나짜리 표를 말없이 만들지 않고
    무엇을 해야 하는지와 **붙일 수 있는 필드 목록**을 함께 알립니다.
  - 검사 10개를 붙였습니다. 그중 하나는 이 방식의 값 전부가 걸린 자리입니다 —
    **표에 같은 이름의 열이 있어도 표시가 없으면 쓰지 않는다.**

### Fixed

- **식별자의 대소문자만 바꿔도 그 행의 에셋이 지워지던 것.** 굽기가 확정한 이름 집합이 대소문자를
  가렸는데 에셋 저장소는 가리지 않습니다. 표의 `Sword`를 `sword`로 고치면 ① 기존 `Sword.asset`을
  찾아서 갱신하고 ② 확정 목록에는 `sword`만 담고 ③ 정리가 폴더의 `Sword`를 **표에서 사라진 행**으로
  보고 지웠습니다. 리포트에는 `갱신 1 / 삭제 1`이 찍혀 성공처럼 읽혔습니다.
  - 겹침 판정(`CsvIdClaims`)은 이미 대소문자를 무시하고 있었습니다. **정리 판정만 빠져 있었습니다.**
  - 그룹 식별자와 참조 이름 색인도 같은 이유로 대소문자를 무시하도록 맞췄습니다. 열 이름·필드
    이름·열거형 값이 전부 그러한데 이 셋만 달랐습니다.

- **닫히지 않은 큰따옴표 하나가 뒤의 행을 통째로 삼키던 것.** 파서가 `inQuotes` 상태로 파일 끝에
  닿아도 아무 말을 하지 않고, 남은 텍스트 전부를 필드 하나로 확정했습니다. 20행짜리 표의 5행에
  `he said "hi` 하나가 들어가면 **파싱된 행이 5개**가 되고, 사라진 15행의 에셋은 `DeleteMissing`
  기본값(`true`)에 따라 정리 대상이 됐습니다.
  - 파서 단계에서 없어지므로 '건너뜀'으로도 세어지지 않아 **어디에도 흔적이 남지 않았습니다.**
  - 이제 표를 읽지 **못한** 것으로 다뤄 굽지도 지우지도 않고, 따옴표가 시작된 줄 번호를 알립니다.

- **"열 이름이 틀리면 멈춥니다"가 사실이 아니던 것.** 멈추는 것은 `RequiredColumns`가 빠졌을 때뿐인데
  그 목록은 기본 선언에서 **식별자 열 하나**였습니다. `Title`을 `Titel`로 잘못 적으면 경고 한 줄 없이
  지나갔고, 새로 만들어지는 에셋에는 C# 기본값이 구워졌습니다.
  - 이제 자동 연결 경로에서도 리포트에 남습니다. 표시를 손으로 붙인 것은 경고, 자동 연결은 알림입니다 —
    자동 연결에서 열이 없는 필드는 정상이라 같은 무게로 경고하면 잡음만 쌓입니다.
  - 표기가 거의 같은 열을 찾았을 때의 문구가 **"대소문자가 다릅니다"**였는데, 대소문자는 헤더 대조가
    이미 흡수하므로 그 경고는 언제나 공백·밑줄·하이픈 차이였습니다. 원인을 바르게 적습니다.
  - README·영문 매뉴얼·Quick Start 세 문서가 모두 같은 잘못을 적고 있어 함께 고쳤습니다.

- **같은 이름의 열이 둘일 때 앞 열의 값이 사라지던 것.** 행 사전이 이름으로 값을 담아 뒤 열이 앞 열을
  덮었고, 경고가 없었습니다. 시트에서 열을 복제하면 바로 나오는 모양입니다. 이제 리포트에 남깁니다.
  대소문자만 다른 열도 같은 열로 봅니다. **멈추지는 않습니다** — 뒤 열의 값은 정상이라, 멈추면
  멀쩡한 표까지 반영되지 않습니다.

- **범위를 넘는 수가 경고 없이 잘려 들어가던 것.** `long`으로 읽어 `longValue`에 넣고 있어서 대상이
  `int`면 Unity가 조용히 잘랐습니다 — 셀의 `2147483648`이 `2147483647`로 구워졌습니다. `float`도
  마찬가지로 `1e40`이 `Infinity`가 됐습니다.
  - 이제 다른 타입과 같게 **기존 값을 남기고 오류를 냅니다.** `int`·`uint`·`short`·`ushort`·
    `byte`·`sbyte`·`char` 전부 대상 필드의 범위로 봅니다.
  - 표에 일부러 적은 `Infinity`·`NaN`은 그대로 받습니다. 사람이 뜻을 밝힌 값입니다.

- **구분자만 남은 셀이 손으로 저작한 목록을 비우던 것.** `;;` 같은 셀은 토큰이 0개라 `arraySize = 0`이
  그대로 실행됐고 경고도 없었습니다. 그런 셀은 내보내기가 만들어 냈습니다 — 아래 항목 참고.
  - 이제 셀에 뭔가 적혀 있는데 읽을 원소가 없으면 목록을 건드리지 않고 오류를 냅니다.
  - 비우는 것이 뜻이라면 `[CsvColumn(OverwriteWhenEmpty = true)]`로 밝히면 됩니다.

- **내보내기가 되읽을 수 없는 열을 만들던 것.** `CsvTemplate`은 표를 만들 때 `CanAuthor`로 걸러
  "되읽을 수 있는 열만" 만드는데, 내보내기는 그 판정을 쓰지 않았습니다. `List<중첩클래스>` 같은
  필드가 빈 문자열로 나가 `;;`만 남은 셀이 원본 표에 기록됐고, 그 셀을 다시 구우면 목록이 비었습니다.
  **두 자리가 같은 함수를 부르게** 했습니다.

- **내보내기가 굽기와 무관한 열을 원본 표에서 지우던 것.** 헤더를 선언에 있는 열로만 다시 써서,
  메모 칸이나 `[CsvIgnore]` 필드의 열처럼 사람이 적어 둔 것이 내보내기 한 번에 사라졌습니다.
  이 패키지 자신이 "A열이 메모 칸이고 B열이 `Id`인 표"를 정상적인 모양으로 적고 있습니다.
  이제 **원본에만 있는 열은 값까지 그대로 옮겨** 보존합니다.

- **저장하지 않은 씬의 배선을 못 보고 지우던 것.** 참조 조사는 `AssetDatabase.GetDependencies` 하나로
  이뤄지는데, 그 답은 **디스크에 저장된 내용**으로 만들어집니다. 씬에 방금 손으로 꽂았고 아직
  저장하지 않은 배선은 거기 없어서, 그 상태로 정리하면 "참조 없음"이 나왔습니다.
  - 이제 저장하지 않은 씬이나 열려 있는 프리팹 스테이지가 있으면 `ReferenceScanBlocked`가 이유를
    돌려주고 **아무것도 지우지 않습니다.** 계약과 보존 경로는 이미 있던 것이라 조건만 채웠습니다.

- **BOM이 붙은 파일이 엄격 검사를 건너뛰던 것.** UTF-8 BOM이 붙어 있으면 치환 폴백으로 읽어, 본문이
  UTF-8이 아니어도 깨진 바이트가 `U+FFFD`로 조용히 바뀌며 통과했습니다. (그 때문에 아래에 있던
  `catch`가 도달 불가능한 죽은 코드였습니다) 이제 BOM이 붙어도 엄격판으로 읽습니다.

- **같은 이름의 표가 둘일 때 말없이 하나를 골라 굽던 것.** 어느 것을 읽을지 `FindAssets`의 검색
  순서가 정했고, 그 순서는 보장되지 않습니다. 산출물 폴더를 적지 않은 선언은 그 경로를 기준으로
  폴더까지 정하므로 굽는 자리도 함께 옮겨 다녔습니다. 샘플을 새 판으로 다시 가져오면 바로 이
  상태가 됩니다. 이제 **경로순으로 못박고**, 무엇을 골랐고 무엇이 더 있는지 전부 적어 알립니다.

- **"CSV 루트 설정을 확인하십시오"가 엉뚱한 곳을 가리키던 것.** 표 조회는 프로젝트 전체를 이름으로
  찾습니다 — CSV 루트 설정과 무관합니다. 사람은 멀쩡한 값을 들여다보게 됩니다.


- **숫자로 보이는 문자열 셀이 조용히 손상되던 것.** 파서가 셀을 읽는 자리에서 값을 보고 타입을
  정하고 있었습니다(`int → float → string`). 그래서 문자열 필드로 가는 값이 한 번 숫자가 됐다가
  문자열로 되돌아오면서 **원문을 잃었습니다** — `007`은 `7`이, `1.10`은 `1.1`이, `1e3`은 `1000`이,
  20자리 식별자는 `1E+20`이 됐습니다.
  - 경고가 없었습니다. 내보내기와 엮이면 **스스로 번졌습니다** — 에셋의 `007`을 표로 뽑고 다시
    구우면 에셋이 `7`이 되고, 어긋남 확인은 그 표를 영원히 "바뀌는 것 있음"으로 보고했습니다.
  - README가 "값만 보고 타입을 정하는 도구가 가장 많이 틀리는 자리인데, 여기서는 필드가 기준이라
    어긋나지 않습니다"라고 적고 있었는데, **파서가 필드를 보기 전에 이미 정하고 있었습니다.**
  - 이제 표 경로는 **셀 원문을 그대로** 들고 있습니다. 타입은 대상 필드가 정합니다.
    `GetInt`/`GetFloat`는 원래 문자열에서 파싱하므로 달라지는 것이 없습니다.
  - 예전부터 있던 `CsvReader.Read`는 boxed 값을 그대로 돌려줍니다. 부르는 쪽이 꺼내 쓰고 있을 수
    있어 동작을 조용히 바꾸지 않았습니다. 손실이 나는 쪽은 그 경로뿐이고, 문서에 적어 두었습니다.
- **UTF-8이 아닌 표가 깨진 글자로 통과하던 것.** Windows Excel의 `CSV(쉼표로 분리)` 저장은 UTF-8이
  아니라 시스템 기본 인코딩(한국어 Windows는 CP949)입니다. 그 파일을 읽으면 예외가 나는 것이 아니라
  **글자가 깨진 채로 통과**했고, 깨진 값이 그대로 에셋에 구워진 뒤 다음 내보내기가 원본 표까지 덮었습니다.
  - 이제 **디스크의 바이트를 직접 보고** 판정합니다. `TextAsset`은 내용을 언제나 UTF-8로 해석하므로
    이 문제를 볼 수 없는 자리였습니다.
  - UTF-8이 아니면 **굽지 않고 멈춥니다.** 시스템 코드페이지로 읽어 주면 같은 표가 사람의 기계에 따라
    다른 값을 굽습니다. 메시지에 Excel에서 고치는 법을 함께 적습니다.
  - BOM이 붙은 **UTF-16**도 읽습니다. BOM 없는 UTF-16은 UTF-8 검사에 걸리지 않아(NUL이 UTF-8에서
    유효한 바이트입니다) 따로 봅니다.
- **참조가 남은 에셋을 지키는 안전장치가 새던 것.** 조사 대상이 `Assets/` 아래의 씬·프리팹·`.asset`
  셋뿐이었습니다. 그 목록에 없는 확장자가 산출물을 붙잡고 있으면 조사 결과가 "참조 없음"이 되어
  **지워졌습니다.** Timeline(`.playable`)·프리셋(`.preset`)·애니메이터가 그런 자리입니다.
  - 이제 **담을 수 없다고 확신할 수 있는 것만 뺍니다.** 그림·소리·영상·모델·글꼴·스크립트·순수
    텍스트·셰이더가 그것이고, 나머지는 전부 봅니다. 세는 쪽으로 틀리면 데이터가 사라지고, 빼는
    쪽으로 틀리면 값을 조금 더 치를 뿐입니다.
  - **임베디드·로컬 패키지 안도 봅니다.** 자기 코드를 `Packages/` 아래로 갈라 둔 프로젝트에서
    그 안의 프리팹이 산출물을 참조합니다. 레지스트리 패키지는 읽기 전용이라 그럴 수 없어 뺍니다.
- **시트 동기화의 실패가 아무 데도 남지 않던 것.** 받기를 기다리지 않고 던져 놓는 구조라
  그 안에서 난 예외가 **관측되지 않은 Task 예외**로 사라졌습니다. 표를 Excel로 열어 둔 채
  '받기'를 누르면 파일 쓰기가 막히는데 — 가장 흔한 사용 방식입니다 — 사람이 보는 것은
  "아무 일도 일어나지 않음"이었습니다. 이제 실패가 로그로 나오고, 파일이 잠긴 경우는
  **무엇을 확인해야 하는지**까지 적습니다.
- **응답이 오지 않으면 동기화가 영영 '도는 중'으로 남던 것.** 요청에 시한이 없었습니다.
  한 번 걸리면 다음 받기가 통째로 막히고 에디터를 다시 켜기 전에는 풀리지 않았습니다.
  받기 60초, 토큰 발급 30초를 걸었습니다.
- **샘플이 없는 메뉴를 가리키던 것.** Quick Start의 3단계가 `Rebuild All Data`를, 되돌리기 절이
  `ScriptableObject를 표로 내보내기`를 안내했는데 둘 다 존재하지 않는 이름이었습니다.
  사는 쪽이 가장 먼저 여는 문서입니다. 문서의 메뉴 이름과 실제 `[MenuItem]`을 전부 대조해 맞췄습니다.
- **같은 이름의 에셋이 여럿일 때 아무거나 골라 배선하던 것.** 이름 색인이 사전 한 칸을 덮어쓰고
  있어서, 어느 것이 붙을지는 `AssetDatabase.FindAssets` 의 **검색 순서**가 정했습니다.
  그 순서는 보장되지 않으므로 같은 표를 다시 구우면 다른 에셋이 붙을 수 있었고,
  **아무 말도 남지 않았습니다.** 못 찾는 것보다 나쁜 종류의 실패입니다 — 사람 쪽에서는
  배선이 된 것처럼 보이기 때문입니다.
  이제 **고르지 않고** 후보 경로를 전부 적어 알리며, 필드 값은 그대로 둡니다.
  - **푸는 길을 함께 적습니다.** 무엇이 잘못됐는지만 말하면 사람은 같은 자리에서 다시 막힙니다.
  - **셀에 에셋 경로를 적을 수 있습니다.** `Sword` 대신 `Assets/Data/Items/Sword.asset`.
    표가 어느 것인지 직접 말하는 길이라, 나중에 같은 이름이 하나 더 생겨도 흔들리지 않습니다.
  - 찾지 못한 경우의 문구에도 **"그대로 둡니다"** 를 넣었습니다. 값을 비우지 않는다는 사실이
    메시지에 드러나 있지 않았습니다.
- **앞 열이 빈 행이 통째로 사라지던 것.** 파서가 빈 줄을 거를 때 **첫 물리 열**만 보고 있었습니다.
  구분자만 있는 줄(`,,,`)을 막으려던 것인데, 식별자가 첫 열이 아닌 표 — 이를테면 A열이 메모 칸이고
  B열이 `Id`인 표 — 에서는 **메모를 비워 둔 행이 전부 없어졌습니다.**
  건수로도 드러나지 않습니다. 파서 단계에서 없어지므로 굽기 쪽은 그 행의 존재조차 모르고,
  빈 식별자 경고에도 건너뜀 집계에도 잡히지 않습니다. 어디에도 흔적이 남지 않는 손실이었습니다.
  이제 **모든 셀이 빈 행만** 건너뜁니다. 한 칸이라도 값이 있으면 행으로 삼습니다.
- **목록 필드만 잘못된 셀에 기존 값이 날아가던 것.** 이 패키지의 규칙은 *"값을 해석하지 못하면
  기존 값을 남긴다"* 인데, 배열·리스트에서만 반대로 동작했습니다. 채우기 전에 길이부터 잘라 놓고
  원소를 하나씩 구웠기 때문에, 셀 하나에 오타가 나면 인스펙터에서 저작한 목록이 **통째로 기본값이
  됐습니다.** 같은 오타가 `int` 필드에서는 경고만 남기고 값을 지키던 것과 갈렸습니다.
  이제 원소를 미리 다 확인하고, 하나라도 변환되지 않으면 **목록을 건드리지 않습니다.**
  판정은 굽기와 같은 변환기에게 맡깁니다 — 여기에 파싱 규칙을 한 벌 더 적으면 미리 본 결과와
  실제로 굽는 결과가 갈라집니다.
- **README 의 내보내기 메뉴 이름이 실제와 달랐습니다.** `ScriptableObject를 표로 내보내기` 라고
  적혀 있었지만 실제 항목은 `에셋을 표로 내보내기` 입니다. 0.13.1 이 훑고 지나간 자리에 하나 남아
  있었습니다. (같은 문서의 메뉴 표에는 바르게 적혀 있어 서로 어긋나 있었습니다)

### Changed

- **사용자에게 보이는 것을 전부 영문으로 옮겼습니다.** 메뉴 10개, 대화상자, 진행 막대, 로그,
  오류 메시지, 리포트 — 294곳. 여기에 **XML 문서 주석 2,331줄**(구매자의 IntelliSense 에 뜹니다),
  README, 영문 매뉴얼, Quick Start 샘플(표 데이터 포함)까지.
  - **반쯤 번역된 상태가 가장 나빴습니다.** 영문 매뉴얼이 한국어 메뉴 이름을 지시하고 있었고,
    `CsvAssetId` 만 영어라 한국어 리포트 안에서 언어가 섞였습니다.
  - 메뉴 이름이 바뀌었습니다: `CSV 파이프라인` → `Pipeline Window`, `전체 다시 굽기` →
    `Rebuild All Tables`, `에셋을 표로 내보내기` → `Export Assets to Tables`,
    `표와 산출물이 어긋나는지 확인` → `Check for Drift`, `스크립터블 오브젝트로 표 만들기` →
    `Create Table from ScriptableObject`, `이 타입의 표 만들기` → `Create Table for This Type`,
    `Google Sheet에서 받기` → `Pull from Google Sheets`, `Google Sheet와 비교만` →
    `Compare with Google Sheets`, `Google Sheet 설정 만들기` → `Create Google Sheet Settings`.
    문서에 적힌 메뉴가 실제 `[MenuItem]` 과 전부 일치하는지 기계적으로 대조했습니다.
  - **구현 주석(`//`)과 이 변경 기록은 한국어로 둡니다.** 전자는 유지보수용이고 구매자에게 닿지
    않으며, 후자는 이력이라 번역하면 원문이 사라집니다.
  - Quick Start 의 `Quests.csv` 에 UTF-8 BOM 을 붙였습니다. 이 패키지 자신의 기본값이 BOM 을
    붙이는 것이고 그 이유를 "없으면 Excel 이 깨뜨려 연다" 라고 적어 두었는데, 샘플 표만 그 규칙
    밖에 있었습니다.

- **굽기 비용.** 3,000행 × 41열 표 실측(Unity 2022.3.62f3, 배치 모드):

  | | 전 | 후 |
  |---|---|---|
  | 첫 굽기 (산출물 0개) | 151.9초 | **74.4초** |
  | 다시 굽기 — 값이 그대로 | 32.6초 | **1.2초** |
  | 다시 굽기 — **한 행만** 바뀜 | 25.4초 | **1.2초** |
  | 다시 굽기 — 전 행 값이 바뀜 | 32.5초 | 32.9초 |

  둘을 고쳤고, 값이 다른 데서 왔습니다.

  - **값이 달라진 행만 더럽힙니다.** 예전에는 값이 그대로여도 모든 행을 더럽혔고, 굽기 끝의
    `SaveAssets`가 그 전부를 다시 썼습니다. 한 칸만 고쳐도 3,000개를 다시 쓰던 자리입니다 —
    **일상적인 편집의 비용은 거의 전부 여기였습니다.** `ApplyModifiedPropertiesWithoutUndo`가
    실제로 바뀐 것이 있을 때만 `true`를 돌려주므로 그 답을 그대로 씁니다.
  - **굽기 루프가 되임포트를 미루는 범위 안에서 돕니다.** 없을 때는 행마다 에셋 파이프라인이
    한 번씩 돌았습니다. 이쪽은 **새로 만드는 경로**에서 값이 큽니다(첫 굽기 2배).
    게이트웨이 계약에 `BatchEdits()`를 두어 **겹쳐 열 수 있게** 했습니다 — 전체 다시 굽기처럼
    바깥에서 이미 열어 둔 채 표마다 다시 여는 경로가 있어, 안쪽이 닫힐 때 저장소가 먼저 풀리면
    그때부터 다시 행마다 돕니다. 정리(삭제)는 범위 밖에서 합니다.

  **전 행이 정말 바뀐 경우는 그대로입니다**(32.9초). 3,000개를 실제로 다시 써야 하므로 줄일 것이
  없습니다. 첫 굽기 74.4초도 한 번 치르는 값입니다.

- `package.json`의 상품 설명이 **"never included in player builds"**라고 적고 있었습니다. 사실이
  아닙니다 — 속성 어셈블리는 빌드에 들어가고, README가 그 이유를 정확히 설명하고 있습니다.
  어긋난 것은 이 한 줄뿐이라 그것을 고쳤습니다.

- README의 asmdef 안내가 커스텀 임포터를 쓰는 경우에 정반대를 지시하고 있었습니다
  (`CsvPipeline.Editor`를 참조하지 말라고 했는데, 문서가 내미는 예제가 그 어셈블리를 씁니다).
  데이터 타입용과 **에디터 전용 asmdef**용으로 갈랐습니다.

- README가 "GUID가 사라지면 git으로도 못 돌아옵니다"라고 적고 있었습니다. `.asset`과 `.meta`를
  **함께** 되돌리면 GUID까지 그대로입니다. 사고가 났을 때 복구를 포기하게 만드는 문장이라 고쳤습니다.


- **이 도구가 쓰는 표에 UTF-8 BOM을 붙입니다.** (기본 켜짐 · Project Settings ▸ CSV Pipeline)
  BOM이 없는 UTF-8 CSV를 Excel이 열면 시스템 기본 인코딩으로 읽어 한글이 깨집니다. 내보내기의
  목적이 "시트에서 다시 저작하는 것"인데 그 파일이 깨져 보이면 기능이 없는 것과 같습니다.
  내보내기·표 만들기·시트 받기가 모두 이 설정을 따르고, 읽는 쪽은 BOM 유무와 무관합니다.
  **처음 한 번은 내보낸 표 전부에 3바이트 차이가 나는 diff가 생깁니다.** 이후로는 잡음이 없습니다.
  (시트 스냅숏은 사람이 열어 볼 파일이 아니라 그대로 둡니다 — 따라가게 하면 설정을 바꾼 순간
  모든 표가 "로컬에서 수정됨"으로 잡힙니다)
- **표를 읽지 못한 이유를 알려 줍니다.** 파일이 없는 것·글자를 읽지 못한 것·데이터 행이 없는 것이
  전부 같은 침묵으로 끝나고 있었습니다. 이제 셋을 갈라 말하고, 글자를 읽지 못한 표는 굽기가
  **오류로 보고**하고 창에서도 손볼 것으로 올라옵니다. 행이 없는 표는 아직 저작이 시작되지 않은
  것이라 그대로 조용합니다.
- **어셈블리 정의(asmdef)를 쓸 때 무엇을 참조해야 하는지 문서에 적었습니다.** 데이터 타입이 자기
  asmdef 안에 있으면 `CsvPipeline` 참조가 필요한데 문서 어디에도 없었습니다. 상용 프로젝트에서는
  asmdef를 쓰는 것이 보통이라, 사는 쪽이 가장 먼저 막히는 자리였습니다.
- **창의 도움말(`?`) 단추가 패키지 안의 문서를 엽니다.** 공개 저장소의 README를 브라우저로 열고
  있었는데, 저장소가 비공개가 된 뒤로 그 주소는 라이선스를 가진 사람에게도 404입니다.
- **표시가 붙은 것만 받는 선언에서, 표시한 열이 표에 없으면 이제 경고합니다.** 자동 연결에서는
  열이 없는 필드가 흔한 일이라 조용히 넘기는 것이 맞지만, `[CsvColumn]`을 손으로 붙였다면
  그 열을 **달라고 적어 둔 것**이라 없는 쪽이 실수입니다. 그동안은 표에 열을 만들었다고 믿은 채로
  값이 계속 그대로 남았습니다. 대소문자만 다른 열이 있을 때의 안내는 그대로입니다.
- **관계없는 에셋을 임포트할 때마다 프로젝트 전체를 뒤지던 것.** 임포트 통지 처리가 값싼
  파일명 판정보다 **먼저** 산출물 폴더를 묻고 있었습니다. `OutputFolder`를 선언에 적지 않은
  `[CsvAsset]` 타입에게 그 물음은 프로젝트 전체 검색(+ CSV 루트 재귀 훑기)인데, 이 통지는
  프로젝트의 **모든** 임포트마다 오고 스키마 수만큼 되풀이됩니다. 텍스처 하나를 넣어도
  치르던 값입니다. 이제 이 표가 지워졌을 때만 묻습니다.

- **라이선스가 MIT에서 상용 라이선스로 바뀌었습니다.** 개발자 1인당 1라이선스이고, 프로젝트 수에는
  제한이 없으며, 구워진 에셋과 `Runtime/`의 속성 어셈블리는 빌드에 넣어 상업적으로 배포할 수 있습니다.
  금지되는 것은 **소스 자체의 재배포·재판매·공개**입니다. 전문은 `LICENSE.md`에 있습니다.
  - **0.13.2 이하는 계속 MIT입니다.** 이미 준 권리는 거둘 수 없습니다. 0.14.0부터가 새 조건입니다.
  - `package.json`의 `documentationUrl`·`changelogUrl`·`licensesUrl`을 **뺐습니다.** 셋 다 비공개가 된
    저장소를 가리켜 사는 쪽에서는 404였습니다. 이 항목들이 없으면 Package Manager가 패키지 안의
    `Documentation~`·`CHANGELOG.md`·`LICENSE.md`를 대신 엽니다. 배포본에 들어 있는 사본이라
    인터넷도 접근 권한도 필요 없습니다.
  - 창의 도움말(`?`) 단추도 같은 이유로 **패키지 안의 문서**를 엽니다. 도움말이 오류 페이지를 여는 것은
    문서가 없는 것보다 나쁩니다 — 사는 쪽은 문서가 사라졌다고 읽습니다.
  - 설치 안내를 저장소 주소에서 **로컬 패키지 경로**로 바꿨습니다.

## [0.13.2] - 2026-08-21

### Fixed

- **패키지의 검사 픽스처가 남의 프로젝트 목록에 올라오던 것.** 실제 게임 프로젝트에 이 패키지를
  끼워 넣고 드리프트 검사를 돌려 보니, 픽스처 넷이 **"못 읽음"** 으로 함께 보고됐습니다.
  픽스처가 가리키는 표는 검사가 도는 동안에만 메모리에 있으니 당연히 못 찾습니다.
  목록이 지저분해지는 것으로 끝나지 않습니다 — 굽기 경로도 같은 목록을 쓰므로, 남의 프로젝트에서
  **있지도 않은 표를 구우려 듭니다.**
  `testables` 를 넣지 않아도 이렇게 됩니다. 끼워 넣거나 서브모듈로 둔 패키지의 검사 어셈블리는
  그냥 컴파일되기 때문입니다.
  발견(`CsvSchema.All` · `CsvImportDefinition.DiscoverAll`)이 이제 검사 어셈블리를 건너뜁니다.
  판정은 **무엇을 참조하는지**로 합니다(`CsvAssemblies`) — Unity 가 표시를 주지 않고, 이름 규칙으로는
  그렇게 이름 붙인 것만 걸리기 때문입니다. **모르면 보통 코드로 봅니다** — 걸러 내는 쪽으로 틀리면
  남의 표가 사라집니다.
  `CsvSchema.For` 로 이름을 대는 길은 그대로입니다. 발견에서 뺀 것이지 못 쓰게 한 것이 아닙니다.

## [0.13.1] - 2026-08-21

제출을 막고 있던 **하나뿐인 하드 블로커**를 없앴습니다. 코드는 건드리지 않았습니다.

### Added

- **영문 문서** `Documentation~/manual-en.md`. 스토어 제출 요건(코드가 든 패키지는 문서가
  필수이며 "comprehensive" 여야 함)에 맞춰, 설치부터 시트 연동·드리프트 검사·직접 만든 임포터
  검사까지 전부 담았습니다. **한국어 README 를 바꾸지 않고 나란히 둡니다** — 소스와 UI 의 언어는
  되돌리기 번거로운 별개의 결정이라, 문서 요건만 먼저 풀었습니다.
  영문 문서는 UI 가 한국어라는 사실도 함께 적어 둡니다.
- **고지 세 가지**를 README 와 영문 문서 양쪽에 적었습니다. 스토어 심사가 묻는 것들입니다.
  - **네트워크** — 접속하는 곳은 `docs.google.com` 과 `oauth2.googleapis.com` **둘뿐**이고,
    연동을 켜기 전에는 아무 데도 아무것도 보내지 않습니다. 자동 받기는 기본이 꺼짐입니다.
  - **자격증명 보관** — 서비스 계정 키는 지정한 경로에서 읽기만 하고, 프로젝트로 복사되지도
    빌드에 들어가지도 로그에 실리지도 않습니다. 설정에 남는 것은 경로뿐입니다.
  - **AI 보조** — 이 패키지는 AI 도구의 보조를 받아 작성했습니다. (2026-05-20 갱신된 스토어
    가이드라인이 요구하는 고지입니다)

### Fixed

- **README 의 메뉴 표가 실제와 어긋나 있었습니다.** `미리보기 (굽지 않고 확인)` ·
  `Rebuild All Data` · `ScriptableObject를 표로 내보내기` · `Google Sheet 설정 에셋 만들기` ·
  `Google Sheet 설정 폴더 열기` 다섯 줄이 지금 없는 메뉴를 가리켰고, 창을 여는 메뉴는 빠져 있었습니다.
  창으로 옮겨 간 것들을 문서가 따라가지 못한 자리입니다.

## [0.13.0] - 2026-08-21

참조 조사를 **글자 읽기에서 의존성 그래프로** 바꿨습니다. 이 패키지의 1번 안전장치가
직렬화 설정에 따라 통째로 꺼지던 것을 근본에서 없앤 것이고, 표마다 프로젝트 전체를 다시 훑던
비용도 함께 사라집니다.

### Fixed

- **이진 직렬화 프로젝트에서 참조 조사가 아무것도 못 찾던 것.** v0.10.0 은 그 상황을 *감지해서
  멈추는* 데까지였습니다. 이제 `AssetDatabase.GetDependencies` 에 물으므로 **직렬화 모드와
  무관하게** 답합니다. 재 본 값입니다 — 같은 프로젝트, 같은 참조 300건에 대해:

  | 직렬화 | 옛 방식 (글자로 읽기) | 새 방식 (의존성 그래프) |
  |---|---|---|
  | Force Text | 300 / 300 | 300 / 300 |
  | Mixed (비-텍스트) | **0 / 300** | **300 / 300** |

  프로젝트 설정이 붙잡고 있는 **프리로드 에셋**도 `PlayerSettings.GetPreloadedAssets()` 로
  함께 봅니다. 예전에는 `ProjectSettings/*.asset` 을 글자로 읽었는데, 그것도 같은 이유로
  믿을 수 없었습니다.

### Changed

- **참조 조사 결과를 들고 있습니다.** 표 한 장을 정리할 때마다 프로젝트 전체를 다시 묻던 것이
  이 도구의 가장 큰 비용이었고, 창의 훑기는 표 수만큼 그것을 되풀이했습니다. 한 번 모아 두고
  **에셋이 하나라도 바뀌면 버립니다** — 낡은 답은 있는 참조를 못 보고 지우는 쪽으로 틀립니다.
  `ICsvAssetGateway.InvalidateCaches` 가 그 계약이고, `CsvAssetCacheHook` 이 부릅니다.
- **표 파일 경로 조회에도 같은 캐시**를 답니다. 프로젝트 전체 검색이라 창이 표마다 한 번씩
  부르면 그대로 비용이 됩니다.
- **`CsvImportDefinition.DiscoverAll` 이 어셈블리 훑기를 되풀이하지 않습니다.**
  찾아 둔 것은 **인스턴스가 아니라 타입**입니다 — 정의는 굽는 동안 취소 상태를 지니므로
  나눠 쓰면 지난 실행의 상태가 따라옵니다.
- 디스크에 쓴 값을 글자로 확인하는 검사가 `Force Text` 가 아닌 프로젝트에서 스스로 건너뜁니다.
  이진 파일에 글자가 없는 것은 이 패키지의 결함이 아닙니다.
- 검사 220건. 통합 검사에 **다른 에셋이 붙잡고 있으면 지우지 않는다**를 더했습니다 —
  메모리 게이트웨이로는 확인할 수 없는, 진짜로 물어봐야 하는 자리입니다.

## [0.12.0] - 2026-08-21

간판 기능인 `[CsvAsset]` 을 **실제 게임 프로젝트에 붙여 보다가** 두 가지가 드러났습니다.
하나는 이 기능이 애초에 쓸 수 없는 상태였다는 것이고, 다른 하나는 표 하나 때문에 코드를
남겨야 하는 빈 자리가 있었다는 것입니다.

### Fixed

- **`[CsvAsset]` 을 게임 데이터 타입에 붙일 수 없었습니다.** 속성들이 에디터 전용 어셈블리에
  있었는데, 게임의 ScriptableObject 는 런타임 타입이라 그 어셈블리를 볼 수 없습니다.
  더 나쁜 것은 **에디터에서는 컴파일된다**는 점입니다 — 플레이어 빌드에서야
  `CS0246: The type or namespace name 'CsvPipeline' could not be found` 로 터집니다.
  만드는 동안에는 멀쩡하다가 **출시할 때 깨지는** 결함이었습니다.
  선언용 속성 셋을 런타임 어셈블리 `CsvPipeline` 으로 옮겼습니다. 실행되는 코드는 없고
  `UnityEngine` 조차 참조하지 않습니다. **검사가 그 자리를 지킵니다** — 파일 하나 옮기면
  되돌아오는 실수라서요.

### Added

- **`CsvDriftCheck` — 표를 고치고 굽기를 잊은 커밋을 잡습니다.**
  표 파일이 바뀐 것은 diff 에 보이지만 산출물이 안 바뀐 것은 보이지 않습니다.
  `-executeMethod CsvPipeline.CsvDriftCheck.Run` 이 종료 코드로 답하고, 아무것도 쓰지 않습니다.
  **판정은 파이프라인 창의 것과 같습니다** — 화면과 CI 가 다른 말을 하면 사람은 어느 쪽도
  믿을 수 없게 됩니다. 에디터에서는
  `Tools ▸ CSV Pipeline ▸ 표와 산출물이 어긋나는지 확인` 입니다.
- **`[CsvAsset(ReconcileByPath = true)]`** — 정리 대조를 이름이 아니라 경로로 합니다.
  코드로 쓴 임포터에는 있던 선택지인데 속성 쪽에 없어, **표 하나 때문에 코드를 남겨야** 했습니다.
- **CI** — `.github/workflows/tests.yml` 이 밀 때마다 EditMode 검사를 돌립니다.
  선언한 최소 판(2022.3)과 Unity 6 LTS 양쪽에서요. `package.json` 의 `unity` 필드는
  지켜지지 않으면 그냥 거짓말입니다. 이 저장소는 패키지이지 프로젝트가 아니라,
  `.ci/project/` 라는 최소한의 껍데기가 저장소 루트를 로컬 패키지로 뭅니다.
  라이선스 활성화는 한 번 사람이 해야 하고, 비밀값이 없으면 **첫 단계에서 그 사실을 말하고 멈춥니다.**

### Changed

- 그리기 검사가 그래픽 장치가 없을 때 `SystemInfo.graphicsDeviceType` 을 보고 물러섭니다.
  컨테이너 CI 에서 진짜 결함을 "화면이 없어서" 로 덮지 않으려는 것입니다.
- 검사 219건. (197 → 219)

## [0.11.0] - 2026-08-21

파이프라인 창을 Unity Editor Design System 기준으로 다시 봤습니다. 가장 컸던 격차는
**키보드만으로는 아무것도 할 수 없었다는 것**이고, 그다음이 **색이 읽히는지 아무도 재 본 적이
없었다는 것**입니다.

### Added

- **키보드로 다 됩니다.** 목록을 `↑` `↓` `Home` `End` 로 고르고, `→` `←` 로 펼치고 접고,
  `Space` 로 뒤집고, `Enter` 로 굽습니다. `Ctrl`(`⌘`)`+F` 로 찾고 `Esc` 로 검색어를 지웁니다.
  검색 칸에서 `↑` `↓` 를 누르면 목록으로 넘어갑니다. 고른 줄은 화면 밖에 있으면 끌어옵니다.
  어느 키가 무슨 뜻인지는 `CsvListKeys` 가 정하고, **검사가 그 뜻을 지킵니다.**
- **줄에서 오른쪽 단추**를 누르면 그 표의 차림표가 열립니다. (굽기 · 표 열기 · 산출물 폴더 ·
  경로 복사) 할 수 없는 항목은 감추지 않고 흐리게 둡니다 — 감추면 차림표 모양이 줄마다 달라져
  손이 기억한 자리가 어긋납니다.
- **보기가 셋**이 되었습니다. `바뀌는 것만` · `손볼 것만` · `전부`. 표가 스무 장을 넘으면
  "바뀌는 것"과 "문제 있는 것"은 다른 목록이어야 합니다.
- **모두 펼치기 · 모두 접기**를 넘침 차림표에 두었습니다.
- **마지막으로 훑은 시각**이 상태 줄에 붙습니다. 지금 보는 것이 언제 것인지 모르면
  밖에서 표를 고친 뒤에도 낡은 화면을 믿게 됩니다.
- **연동 갈래에도 검색**이 생겼습니다. 설정이 스무 개를 넘으면 눈으로 찾을 수 없습니다.
- **줄 맨 왼쪽 상태 막대.** 아이콘·낱말·색에 자리까지 더해, 목록을 세로로 훑을 때 눈이
  한 줄만 따라가면 됩니다.
- **마우스가 올라간 줄이 밝아집니다.** 창이 앞에 있을 때만 마우스 움직임을 받고, 올라간 줄이
  **바뀔 때만** 다시 그립니다. 표 갈래에만 넣었습니다 — 연동 갈래는 줄 자체가 단추가 아니라,
  밝아지면 누를 수 있다고 잘못 말하게 됩니다.

### Fixed

- **색이 읽히지 않았습니다.** 덜 중요한 글자를 반투명(`GUI.contentColor`)으로 덧칠하고 있었는데,
  그 방식은 바탕 스타일의 글자색과 **곱해져** 최종 색을 알 수 없게 만듭니다. 재 보니 어두운 스킨
  2.6:1 로, Unity 가 요구하는 **4.5:1**(US-0173)에 한참 못 미쳤습니다. 색을 다 정해진 값으로 바꾸고
  `CsvPalette` 에 모았습니다. **검사가 두 스킨 모두에서 명암비를 지킵니다.**
- **되돌릴 수 없는 삭제와 고쳐야 할 오류가 같은 빨강이었습니다.** Unity 의 뜻 색 규약대로
  노랑(주의)과 빨강(오류)을 갈랐습니다. 보존·건너뜀도 노랑입니다 — 둘 다 "표에 적힌 대로 되지
  않았다"는 뜻이라 눈에 걸려야 합니다.
- **툴팁이 거의 없었습니다.** 모든 단추·검색 칸·상태 표시에 붙였고, **비활성 단추에는 왜 꺼졌는지**를
  적었습니다(US-0240).
- **검색 칸에 지우기 단추가 없었습니다.** Unity 자신의 도구들이 쓰는 `SearchField` 로 바꿨습니다.
- **창을 좁히면 필드 변경 줄이 가로로 잘렸습니다.** 칸 너비가 고정 픽셀이었습니다. 이제 창 너비를
  따르고, 줄여 보인 값은 **툴팁에 전체가 담깁니다.**
- **창 이름이 메뉴 항목과 달랐습니다.** 메뉴 `CSV 파이프라인 창` / 제목 `CSV Pipeline` 을
  둘 다 `CSV 파이프라인` 으로 맞췄습니다.

### Changed

- 그리기 검사가 **배치 실행에서도 돕니다.** `-nographics` 없이 띄우면 창을 열고 그릴 수 있어,
  늘 건너뛰던 검사 2건이 이제 CI 에서 값을 합니다. 갈래 셋·보기 셋·빈 화면·최소 너비를 덮습니다.
- 검사 197건, 배치 실행에서 **건너뛰는 것 없이** 전부 통과합니다. (154 → 197)

## [0.10.0] - 2026-08-21

말없이 데이터를 잃던 자리 셋을 막았습니다. 셋 다 **경고 한 줄 없이** 일어나던 것들이라,
쓰는 사람이 알아챌 방법이 없었습니다.

### Fixed

- **겹친 식별자를 조용히 삼키던 것.** 같은 `Id` 를 가진 행이 둘이면 두 행이 같은 파일을
  가리켜 **뒤 행이 앞 행을 덮었습니다.** 건수로도 드러나지 않았습니다 — 앞 행은 '생성',
  뒤 행은 '갱신'으로 세어져 정상 임포트와 구분되지 않았기 때문입니다.
  이제 줄 번호를 달아 경고하고, 미리보기에서도 같은 겹침을 먼저 알립니다.
  **대소문자만 다른 식별자도 겹침으로 봅니다.** 윈도우에서는 같은 파일이 되고
  macOS·리눅스에서는 다른 파일이 되어, 그대로 두면 어느 PC에서 구웠느냐에 따라 결과가 갈립니다.
- **이진 직렬화 프로젝트에서 안전장치가 통째로 꺼지던 것.** "참조가 남은 에셋은 지우지 않는다"는
  씬·프리팹을 글자로 읽어 GUID를 찾는 방식이라, Asset Serialization 이 `Force Text` 가 아니면
  **무엇을 물어도 "참조 없음"** 이 나왔습니다. 그 답을 그대로 믿어 씬이 쓰고 있는 에셋을
  경고 없이 지웠습니다. 이제 조사할 수 없다는 것을 감지해 **아무것도 지우지 않고** 이유를 알립니다.
  개발 프로젝트가 `Force Text` 면 아무리 써 봐도 드러나지 않는 자리라, 검사가 그 상황을 흉내 냅니다.
- **파일 이름으로 쓸 수 없는 식별자가 조용히 실패하던 것.** `Item/Sword` · `Q:01` · 앞뒤 공백처럼
  파일 이름이 될 수 없는 값이 그대로 경로에 붙었습니다. 에셋은 만들어지지 않는데 오류도 건너뜀도
  남지 않았습니다. 이제 줄 번호와 이유를 달아 그 행만 건너뜁니다.
  **씻어 주지 않고 거절합니다** — 이름을 자동으로 바꾸면 표에 적힌 것과 에셋 이름이 갈려 더 헷갈립니다.
  금지 문자 목록은 운영체제에서 가져오지 않고 고정했습니다. 그러지 않으면 윈도우에서 거절된 표가
  macOS에서는 통과해 사람에 따라 결과가 갈립니다.

### Added

- `CsvIdClaims` — 한 표 안에서 식별자가 겹치는지 지켜보는 순수 타입입니다.
  굽기와 미리보기가 **같은 판정**을 쓰도록 한 곳에 두었습니다.
- `CsvAssetId` — 식별자를 파일 이름으로 써도 되는지 가리는 순수 타입입니다.
- `ICsvAssetGateway.ReferenceScanBlocked` — 참조 조사를 믿을 수 없으면 그 이유입니다.
  게이트웨이가 "모른다"고 말할 수 있어야 정리가 멈출 수 있습니다.
- `MemoryAssetGateway.WithReferenceScanBlocked` — 조사할 수 없는 프로젝트를 흉내 냅니다.
- 검사 36건. (118 → 154, 배치 실행에서 화면이 필요한 2건만 건너뜀)

### Changed

- `CsvBakeOutcome` 이 줄 번호를 함께 나릅니다. 겹쳤을 때 **어느 행과 부딪쳤는지**를 말하려면
  이 값이 필요합니다.
- 겹침 확인이 `CsvImportDefinition.BakeEach` 의 계약이 되었습니다. 취소 시 정리를 건너뛰는 규칙과
  같은 자리입니다 — 파생 클래스마다 다시 적을 일이 아닙니다.

## [0.9.1] - 2026-08-10

### Fixed

- **에디터를 죽이던 결함.** `Project Settings ▸ CSV Pipeline` 을 열면 몇 분 만에
  `Could not allocate memory: System out of memory!` 로 에디터가 내려앉았습니다.
  `CsvPipelineSettings.ExistsInProject` 가 부를 때마다 프로젝트 전체 검색
  (`AssetDatabase.FindAssets`)을 돌았고, 그 값을 **그리기 안에서** 읽고 있었습니다.
  그리기는 마우스가 움직이는 동안에도 계속 돌기 때문에, 화면에 마우스를 올려 둔 것만으로
  검색과 목록 할당이 끝없이 쌓였습니다. 찾기는 한 번만 하고 결과를 들고 있습니다.
- **같은 값을 그리기에서 읽던 화면이 셋이었습니다.** 설정 제공자·파이프라인 창의 상태 줄·
  설정 갈래 모두 고쳤습니다.
- **`CsvPipelineSettings.InvalidateCache` 를 부르는 곳이 하나도 없었습니다.**
  캐시를 버릴 길이 막혀 있으면 지운 설정을 계속 있다고 말하게 됩니다.
  `CsvPipelineSettingsHook` 이 설정 에셋의 생성·삭제·이동을 보고 버립니다.
- **`GoogleServiceAccount.IsConfigured` 가 그릴 때마다 디스크를 두드리던 것.**
  설정 경로가 그대로면 확인 결과를 그대로 씁니다.

### Added

- `MemoryAssetGateway.FindPathsCount` — 프로젝트를 몇 번 뒤졌는지 셉니다.
  값이 맞는지만 보는 검사로는 위 결함이 잡히지 않아, **몇 번 부르는지**를 검사합니다.
  고치기 전 코드에 대고 돌리면 `1` 을 기대한 자리에서 `200` 이 나옵니다.
- 검사 6건 추가 (112 → 118).

### Changed

- `CsvPipelineSettings.FindAll` 이 `ICsvAssetGateway` 를 거칩니다. 마지막까지 남아 있던
  굽기 경로 밖의 직접 `AssetDatabase` 호출이었고, 거치게 되면서 검사할 수 있게 됐습니다.

## [0.9.0] - 2026-08-10

파이프라인 창의 UI/UX 를 다시 짰습니다. 무엇을 보고 어떻게 판단했는지는
`Documentation~/ui-review.md` 에 있습니다.

### Fixed

- **갈래끼리 스크롤 위치를 함께 쓰던 것.** 표를 아래까지 내린 뒤 시트 갈래로 가면
  항목이 셋뿐인데도 엉뚱한 자리에서 시작했습니다. 갈래마다 따로 들고 있습니다.
- **시트 목록을 매 프레임 다시 찾던 것.** 그리기는 마우스가 움직이는 동안에도 계속 도는데,
  그때마다 프로젝트를 뒤져 설정 에셋을 전부 읽었습니다. 한 번 모아 들고 있다가
  '다시 훑기' 와 설정 생성 때만 버립니다.
- **`OnGUI` 한가운데에서 진행 막대를 띄우던 것.** Layout 과 Repaint 가 서로 다른 것을
  그리게 됩니다. 사람이 눌러서 부른 훑기에서만 띄웁니다.

### Added

- **창 상태가 도메인 재적재를 넘습니다.** 갈래·검색어·필터·펼친 표를 `SessionState` 에
  싣습니다. 전에는 스크립트를 한 줄 고칠 때마다 보던 자리를 잃고 전체 재훑기까지 기다렸습니다.
- **상태 줄**(창 맨 아래) — 갈래마다 흩어져 있던 개수 요약과, 갈래 줄을 차지하던 안전 문구가
  한자리로 내려왔습니다.
- **`CsvPlanStatus`** — 계획을 상태 하나로 줄이는 규칙을 그리기에서 떼어 냈습니다.
  아이콘·색·정렬·필터가 모두 이 판정에 매여 있어, 화면 안에 있으면 확인할 방법이 없었습니다.
- 검사 14건 추가 (98 → 112). 그중 2건은 **화면 있는 에디터에서 창을 실제로 그려 봅니다.**
  IMGUI 의 Begin/End 짝이 어긋나면 컴파일은 통과하고 창만 무너지는데, 그것은 배치 실행으로는
  드러나지 않습니다.

### Changed

- **줄 맨 앞에 상태 칸을 고정했습니다.** 아이콘 + 낱말 + 색 셋을 함께 써 색에만 기대지 않습니다.
  표마다 두르던 상자를 걷고 한 줄 걸러 옅은 바탕으로 바꿨습니다.
- **손볼 것이 있는 표가 위로 옵니다.** 막힘 → 문제 → 삭제 → 변경 → 같음 순이고, 같으면 이름순입니다.
  전에는 이름순뿐이라 오류 난 표가 알파벳 순서에 따라 스크롤 아래에 묻혔습니다.
- **줄 전체가 펼침 단추입니다.** 삼각형을 겨눌 필요가 없습니다.
- **되돌릴 수 없는 것을 넘침 메뉴로 내렸습니다.** `전체 다시 굽기` 와 `에셋을 표로 내보내기` 에
  무슨 일이 일어나는지 적은 확인 대화를 붙였습니다.
- **시트 갈래에서 `전부 비교만` 을 `전부 받기` 왼쪽에 뒀습니다.** 받기는 로컬 표를 덮어쓰므로
  비교가 먼저인 것이 순서입니다.
- **빈 화면을 제목 → 왜 → 다음 행동 하나로 바꿨습니다.** 걸리는 표가 없을 때는 검색어 때문인지
  필터 때문인지 짚어 주고, 원인별 단추를 답니다.
- 간격을 4 / 8 / 12 / 16 네 단으로 통일했습니다. (Unity 자신의 App UI 가 쓰는 계단)
  아이콘은 내장 아이콘을 쓰되 **없으면 글자로 물러섭니다.**

## [0.8.0] - 2026-08-10

아키텍처 리뷰의 리팩토링 로드맵 세 항목을 시행했습니다. 측정한 전후 값은
`Documentation~/architecture-review.md` §5에 있습니다.

### Added

- **`ICsvAssetGateway`** — 파이프라인이 에셋 저장소에 닿는 유일한 통로입니다.
  `UnityAssetGateway`가 실제 구현, `MemoryAssetGateway`가 아무것도 쓰지 않는 구현입니다.
  `CsvAssets.Use(gateway)` 로 범위 안에서만 갈아 끼웁니다. 소비하는 프로젝트도 자기 임포터를
  Unity 없이 검사할 수 있습니다.
- **`SheetDownloader` · `SheetDiff` · `SheetSnapshot` · `SheetSyncScheduler`** —
  받기·비교·사본·시점이 각자의 자리로 갈렸습니다. `SheetDiff`는 순수 함수만 있어 그대로
  검사할 수 있습니다.
- **`CsvImportDefinition.BakeEach`** — 진행·취소·집계·정리를 맡는 굽기 골격입니다.
  파생은 "단위 하나를 어떻게 굽는가"만 답합니다.
- **`CsvImportDefinition.CancelCurrentRun`** — 굽는 중에 더 진행해 봐야 결과가 틀어질 조건을
  알아챈 파생이 안전하게 멈춥니다. 정리도 함께 건너뜁니다.
- 검사 26건 추가 (72 → 98). 굽기 규칙은 이제 임시 폴더 없이 메모리에서 돕니다.

### Fixed

- **시트 비교의 대조 실패 안내가 틀린 숫자를 보여 주던 것.** "줄 수"라면서 색인 크기를
  찍고 있었습니다. 색인은 겹친 식별자를 접으므로, 하필 그 안내가 열리는 상황
  (식별자가 겹치거나 비어 있는 표)에서 양쪽 다 같은 숫자가 나와 아무것도 알려 주지
  못했습니다. 실제 줄 수를 셉니다.

### Changed

- `CsvAssetPipeline`이 게이트웨이에 위임합니다. **공개 API는 그대로라 호출부는 바뀌지 않습니다.**
- "받기"와 "비교만"이 각자 적어 두던 받기·검증 절차가 `SheetFetch` 하나로 합쳐졌습니다.
  88줄·81줄 메서드가 사라졌습니다.
- 파생 임포터 다섯 곳의 `Process`가 골격 위로 올라갔습니다.
  **취소 시 정리를 건너뛰는 규칙이 한 곳에서 강제되고, 검사가 그것을 지킵니다.**

## [0.7.0] - 2026-08-10

### Changed

- **창 하나로 모았습니다** (U2). 기능이 메뉴 항목으로 흩어져 있어 지금 무엇이 어떤 상태인지
  한눈에 볼 자리가 없었습니다. `Tools ▸ CSV Pipeline ▸ CSV 파이프라인 창` 에 갈래 셋을 둡니다.
  **표** — 표마다 무엇이 달라지는지, 검색·필터, 표별 `지금 굽기`.
  **시트 연동** — 설정마다 상태 한 줄과 받기·비교·선택.
  **설정** — 실제 적용되는 경로와 Project Settings 로 가는 길.
- **메뉴를 6개로 줄이고 이름을 한 갈래로 맞췄습니다.** 우선순위로 묶어 구분선이 들어갑니다.
  창 / 굽기·내보내기 / 시트 세 묶음입니다. `Rebuild All Data` 처럼 영문만 남았던 이름도 정리했습니다.
  `설정 폴더 열기` 는 창의 버튼으로 옮겼습니다.

### Added

- `GoogleSheetSync.PullOne` · `FindAll` — 설정 하나만 받거나 목록을 얻습니다. 창이 씁니다.

### Removed

- `CsvPreviewWindow` — `CsvPipelineWindow` 의 '표' 갈래가 그 자리를 대신합니다.

## [0.6.0] - 2026-08-10

조사 문서의 UI/UX 격차 U4~U7을 정리했습니다.

### Added

- **큰 표를 구울 때 진행 막대와 취소** (U6). 200행 이상에서만 뜹니다. 작은 표는 그리는 값이
  굽는 값보다 큽니다. **취소하면 읽은 데까지만 반영하고 정리는 건너뜁니다** —
  아직 읽지 않은 행의 에셋을 "표에서 사라진 것"으로 오해해 지우면 안 되기 때문입니다.
- **연동 설정 인스펙터** (U5). 붙여넣은 주소에서 뽑아 낸 **시트 ID와 탭 번호(gid)를 드러냅니다.**
  gid를 잘못 가리키면 엉뚱한 탭이 조용히 들어오는데, 값을 보여 주지 않으면 알 방법이 없습니다.
  대상 표의 존재 여부, 마지막 동기화 시각, "이 표만 비교"·"브라우저에서 열기" 버튼을 함께 둡니다.
- **미리보기 창의 첫 실행 안내** (U4). 표가 하나도 없을 때 "없습니다"로 끝내지 않고
  붙이는 두 가지 길과 지금 보고 있는 CSV 루트를 알려 줍니다. 설정 화면과 샘플로 가는 버튼도 둡니다.
- `GoogleSheetSync.CompareOne` — 설정 하나만 시트와 비교합니다. 파일은 쓰지 않습니다.

### Documented

- **임포트는 Ctrl+Z 로 되돌릴 수 없습니다** (U7). 의도적으로 지원하지 않습니다.
  한 번의 임포트가 필드 수정·에셋 생성·삭제를 함께 하는데 Unity의 Undo는 필드 수정만 되돌립니다.
  받아 주면 값은 돌아가고 에셋은 남는 어긋난 상태가 되어, 되돌릴 수 없는 것보다 나쁩니다.
  대신 미리보기와 git이 그 자리를 맡습니다.

## [0.5.1] - 2026-08-10

배치 모드로 검사를 실제로 돌려 보고 드러난 것들입니다. 정적 검증으로는 전부 보이지 않았습니다.

### Fixed

- **산출물 폴더를 지운 뒤 다시 구우면 에셋이 빈 값으로 만들어지던 것.**
  지워진 경로에 곧바로 만들면 재임포트가 끼어들어 메모리에만 있던 수정이 버려집니다.
  경로도 폴더도 개수도 맞는데 값만 기본값이라, 사람이 원인을 짚기 어려운 자리였습니다.
  새로 만든 에셋은 저장을 배치 끝까지 미루지 않습니다. (`CsvAssetPipeline.FlushIfCreated`)
- **미리보기가 필드 이름을 열 이름 자리에 보여 주던 것.** 자동 연결은 필드 이름을 열 이름으로
  삼는데, 사람에게는 자기가 표에 적은 표기(`MaxSpeed`)를 보여 줘야 합니다.

### Changed

- 검사가 **매번 새 임시 폴더**를 씁니다. 같은 경로를 한 세션 안에서 지웠다 만들기를 반복하면
  AssetDatabase가 지워진 항목을 붙들고 있어 결과가 실행 순서에 따라 달라집니다.
- 검사 실패 메시지가 에셋의 실제 값과 계획 내용을 함께 냅니다. 무엇이 안 들어갔는지 바로 보여야 합니다.

### Added

- `Documentation~/running-tests.md` — 에디터를 열지 않고 검사를 돌리는 법.
  쓰던 에디터가 프로젝트를 잠그고 있어도 샌드박스 프로젝트로 돌릴 수 있습니다.

## [0.5.0] - 2026-08-10

### Added

- **미리보기 창** — `Tools ▸ CSV Pipeline ▸ 미리보기 (굽지 않고 확인)`.
  표를 지금 구우면 무엇이 생기고·바뀌고·지워지고·보존되는지 **적용 전에** 보여 줍니다.
  이 패키지가 지키는 규칙(참조가 남은 에셋은 지우지 않는다, 빈 셀은 보존한다)은 결과를 봐야만
  확인되는데, 그 결과를 미리 보여 주는 것이 이 창의 목적입니다. **아무것도 쓰지 않습니다.**
- `CsvImportDefinition.Plan(csvPath)` — 굽지 않고 계획만 계산합니다. 베이스 4종이 모두 지원합니다.
  `[CsvAsset]` 로 선언된 표는 **어느 열이 무엇에서 무엇으로 바뀌는지**까지 알려 줍니다.
  사본에 실제 변환기로 구워 본 뒤 비교하므로 미리보기와 실제 결과가 어긋나지 않습니다.
- `CsvImportDefinition.DiscoverAll()` — 직접 작성한 임포터까지 반사로 찾습니다. 미리보기 창의 목록입니다.
- `CsvAssetPipeline.PlanReconcile` — 사라진 산출물을 지우지 않고 삭제 대상과 보존 대상으로 분류만 합니다.
- `CsvValueFormatter` — 필드 값을 셀 문자열로 되돌립니다. 내보내기에 묻혀 있던 것을 꺼내
  미리보기와 공유합니다. 두 화면이 같은 표기를 쓰게 하는 것이 목적입니다.

### Changed

- 값이 하나도 달라지지 않는 행은 미리보기에서 **갱신으로 세지 않습니다.**
  실제 임포터는 그런 행도 갱신으로 세지만, 미리보기에서는 "무엇이 실제로 바뀌는가"가 드러나야 합니다.

## [0.4.0] - 2026-08-10

에셋스토어 조사에서 위험으로 분류했던 항목들을 정리했습니다.

### Changed

- **최소 Unity 버전을 `2022.3` 으로 올렸습니다.** 스토어 신규 제출 기준입니다. (조사 문서의 B1)
- **연동 설정이 하나도 없으면 자동 받기 루프를 걸지 않습니다.** (R2)
  시트 연동을 쓰지 않는 프로젝트에는 매 프레임 콜백이 아예 남지 않습니다.
  설정 에셋이 생기면 임포트 통지가 걸어 주고, 전부 지워지면 뗍니다.
  받을 대상이 없을 때의 재확인 간격도 30초에서 300초로 늘렸습니다.

### Fixed

- **멈춤 깊이가 남으면 자동 임포트가 영영 멈추던 것.** (R1)
  Domain Reload를 끈 프로젝트에서는 정적 값이 살아남습니다. `CsvImport.Suppress()` 의 깊이가
  0으로 돌아오지 못하면 표를 저장해도 아무 일이 일어나지 않고, 사람은 이유를 알 수 없습니다.
  편집 모드로 돌아올 때마다 풉니다.

## [0.3.2] - 2026-08-10

### Fixed

- **내보내기가 방금 쓴 표를 다시 굽던 것.** 파일을 쓴 뒤 `AssetDatabase.Refresh` 를 부르면
  포스트프로세서가 발화해, **그 표를 만들어 낸 바로 그 에셋들을 되굽습니다.**
  결과는 같지만 순전한 낭비이고 로그도 두 배가 됩니다. 쓰는 동안 자동 임포트를 멈춥니다.

### Added

- **`CsvImport.Suppress()`** — 자동 임포트를 잠시 멈추는 범위입니다. `using` 으로 감싸 씁니다.
  표를 여러 장 한꺼번에 써 넣을 때 쓰는 도중마다 되굽는 낭비를 막습니다.
  경로를 직접 넘기는 `CsvImportDefinition.Run` 은 명시적 호출이라 영향을 받지 않습니다.

### Changed

- 왕복 통합 검사가 `LogAssert.ignoreFailingMessages` 대신 **`LogAssert.Expect`** 를 씁니다.
  경고·오류가 나는 것 자체가 그 검사의 기대값이므로, 덮는 대신 명시해 확인합니다.
  덕분에 검사가 강해지고 콘솔도 조용해집니다.

## [0.3.1] - 2026-08-10

### Fixed

- **테스트가 스크립트 참조 없는 에셋을 만들던 것.** 테스트용 ScriptableObject 두 개가
  `TestFixtures.cs` 안에 함께 들어 있었습니다. Unity는 타입 이름과 같은 이름의 파일에서만
  MonoScript를 찾으므로, `AssetDatabase.CreateAsset` 이 `m_Script` 가 빈 에셋을 만들고
  콘솔에 `No script asset for WidgetData` 가 반복됐습니다.
  같은 세션 안에서는 값이 오가 검사는 통과했지만, 도메인 리로드 뒤에는 깨진 에셋입니다.
  타입마다 파일을 나눴습니다. (`BinderTarget.cs` · `WidgetData.cs`)

## [0.3.0] - 2026-08-10

### Fixed

- **자동 연결이 아무 필드도 쓰지 못하던 것.** `[CsvAsset]` 의 AutoMap이 필드 이름으로 열을 찾는데
  그 조회가 대소문자를 가리고 있었습니다. 표는 `MaxSpeed`, 필드는 `maxSpeed` 로 적히는 것이 보통이라
  **정상적인 이름 쓰임새에서 하나도 매칭되지 않았습니다.** `ReadTable` 경로의 헤더 집합과 셀 사전을
  `OrdinalIgnoreCase` 로 바꿉니다. 예전부터 쓰던 `Read` 는 호출부 동작이 바뀌지 않도록 `Ordinal` 유지.
- **내보낸 표의 헤더 표기가 필드 이름으로 바뀌던 것.** `MaxSpeed` 가 `maxSpeed` 로 나가면 내용이 같아도
  매번 "다름"으로 잡히고 시트와 헤더가 어긋나 동기화가 멈춥니다. 원본 표기를 그대로 씁니다.

### Added

- **EditMode 테스트 60여 건** — 파서·표·값 변환기·스키마·쓰기, 그리고 표 → 에셋 → 표 왕복 통합 검사.
  값 변환기와 왕복 경로는 컴파일로 보증되지 않아 실제로 돌려 보는 것이 유일한 확인 방법입니다.
- **`Samples~/QuickStart`** — 임포터 코드 없이 표 한 장이 에셋이 되는 최소 예제.
  Package Manager의 Samples에서 가져옵니다.
- `CsvSchema.For(Type)` — 타입 하나의 스키마를 만듭니다. 어셈블리 전체를 훑지 않습니다.
- `CsvTable.ResolveHeader` — 표에 적힌 그대로의 열 이름을 돌려줍니다.

### Changed

- **`[CsvAsset]` 생성자가 `(fileName, idColumn)` 로 바뀌었습니다.** 산출물 폴더는
  `OutputFolder` 프로퍼티로 옮겼고, **비우면 원본 표 옆의 타입 이름 폴더**를 씁니다.
  설치 위치를 알 수 없는 배포 예제가 그대로 동작해야 해서입니다.
- `CsvTable.FindSimilarColumn` 이 대소문자 대신 **공백·밑줄·하이픈** 차이를 잡습니다.
  대소문자는 이제 매칭이 흡수하므로 그 쓸모가 없어졌습니다.

## [0.2.0] - 2026-08-09

조사에서 드러난 기능 격차 여섯 개를 해소했습니다.
가장 큰 것은 **쓰려면 C# 서브클래스를 써야 했다**는 점이었습니다.

### Added

- **속성 기반 매핑** — `[CsvAsset]` 을 ScriptableObject에 붙이면 임포터 코드 없이 표가 붙습니다.
  필드 이름과 열 이름을 대소문자 무시로 맞추고, `[CsvColumn]` 으로 이름·필수 여부·빈 셀 처리·
  리스트 구분자·참조 검색 폴더를 조정합니다. `[CsvIgnore]` 로 제외합니다.
  문자열·정수·실수·bool·열거형·Vector2/3/4·Color·오브젝트 참조와 그 배열/리스트를 다룹니다.
- **역방향 내보내기** — `Tools ▸ CSV Pipeline ▸ ScriptableObject를 표로 내보내기`.
  `[CsvAsset]` 으로 선언된 타입만 됩니다. 바뀐 파일을 먼저 보여 주고 확인해야 씁니다.
- **비공개 구글 시트** — 서비스 계정 키(JSON)로 액세스 토큰을 받아 붙입니다.
  브라우저 로그인 흐름이 없어 배치 모드에서도 됩니다. 키 내용은 로그에 싣지 않습니다.
- **TSV 지원** — `.tsv` / `.tab` 을 함께 다룹니다. 구분자는 확장자로 정합니다.
  Unity가 `.tsv` 를 TextAsset으로 임포트하지 않으므로 디스크에서 직접 읽는 폴백을 두었습니다.
- **임포트 결과 리포트** — 표마다 한 줄의 로그로 생성·갱신·건너뜀·삭제·보존 건수를 냅니다.
  문제는 원본 줄 번호와 열 이름을 달고 나오며, 로그를 클릭하면 원본 표나 문제가 난 에셋으로 갑니다.
- **열 존재 검증** — 요구한 열이 없으면 **아무것도 반영하지 않고** 오류로 보고합니다.
  빠진 열을 빈 셀로 취급해 조용히 기본값을 굽던 자리를 막습니다.
  대소문자만 다른 열이 있으면 오타로 보고 함께 알립니다.
- `CsvTable` — 행뿐 아니라 헤더를 함께 들고 있는 표. 열 검증의 근거입니다.
- `CsvRow.LineNumber` · `HasColumn` · `SplitList` · `LooksLikeList`.

### Changed

- `CsvImportDefinition.Process` 가 `IReadOnlyList<CsvRow>` 대신 `CsvTable` 과 `CsvImportReport` 를 받습니다.
  베이스 넷을 상속한 코드는 `Bake`/`GetId` 만 구현하므로 영향이 없습니다.
- `CsvAssetPipeline.Reconcile*` 이 리포트에 삭제·보존 건수를 기록합니다.
- `CsvAssetPipeline.FindCsvPath` 가 TextAsset 검색에 실패하면 CSV 루트를 직접 훑습니다. (`.tsv` 때문)

## [0.1.1] - 2026-08-09

### Added

- `package.json`에 `documentationUrl`·`changelogUrl`·`licensesUrl`.
  없으면 Package Manager 창의 상세 패널에 문서 링크 버튼이 아예 뜨지 않습니다.
  (README는 패키지 안에 들어가지만 그 패널이 렌더링해 주지는 않습니다)
- README 설치 절에 비공개 저장소 인증 조건.
  Package Manager는 대화형 인증을 못 하므로 자격증명이 미리 캐시돼 있거나 SSH 키가 있어야 합니다.

## [0.1.0] - 2026-08-09

게임 프로젝트 안에 있던 CSV → ScriptableObject 파이프라인을 독립 패키지로 분리한 첫 배포입니다.

### Added

- `CsvReader` — RFC 4180 따옴표 필드·BOM·CRLF를 처리하는 로케일 독립 파서
- `CsvRow` — 셀 접근·타입 파싱·리스트 분리 접근자
- `SoBaker` — `SerializedObject` 필드 세터 모음. `Set*If` 는 빈 셀에 기존 값을 보존
- `CsvAssetPipeline` — 폴더 보장·에셋 생성/로드·산출물 정리.
  참조가 남은 에셋과 원본이 사라진 폴더는 지우지 않고 경고만 남김
- `AssetNameIndex<T>` — 이름으로 다른 에셋을 참조하는 셀 해석용 인덱스
- 임포터 베이스 4종 — `CsvRowImporter<T>` / `CsvGroupImporter<T>` /
  `CsvPatchImporter<T>` / `CsvSingletonImporter<T>` 와 진입점 `CsvImport.Run<T>`
- `GoogleSheetSync` — 구글 시트에서 CSV 받기·비교. HTML 응답 거부, 헤더 불일치 확인,
  동일 내용 기록 생략, 로컬 수정 감지
- `CsvPipelineSettings` + Project Settings ▸ CSV Pipeline — CSV 루트 등 폴더 위치 지정.
  설정 에셋이 없으면 기본값으로 동작하며 에셋을 자동 생성하지 않음
- `Tools ▸ CSV Pipeline ▸ Rebuild All Data` — 전 CSV 강제 재임포트
